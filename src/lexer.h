/* Minimal lexer logic

while not EOF:
    skip whitespace
    if letter → read identifier
    if digit → read number
    if '"' → read string literal
    if symbol → read symbol token
*/

#ifndef LEXER_H_
#define LEXER_H_

#endif // LEXER_H_